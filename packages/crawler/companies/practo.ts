import { emptyOfficialConfig } from "./_simple.js";

export const practoConfig = emptyOfficialConfig(
  "practo",
  "Practo official careers redirect currently lands on Param.ai endpoints returning 404 for public job APIs.",
);
