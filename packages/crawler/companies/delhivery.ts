import { emptyOfficialConfig } from "./_simple.js";

export const delhiveryConfig = emptyOfficialConfig(
  "delhivery",
  "Delhivery official careers page currently exposes no public job listing feed.",
);
