import { emptyOfficialConfig } from "./_simple.js";

export const nykaaConfig = emptyOfficialConfig(
  "nykaa",
  "Nykaa official careers/jobs route currently redirects to the corporate page and exposes no public job listing feed.",
);
