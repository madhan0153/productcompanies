import { emptyOfficialConfig } from "./_simple.js";

export const yellowAiConfig = emptyOfficialConfig(
  "yellow-ai",
  "Yellow.ai official /careers page currently returns a 404 and exposes no public job listing feed.",
);
