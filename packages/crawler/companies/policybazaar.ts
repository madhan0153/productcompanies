import { emptyOfficialConfig } from "./_simple.js";

export const policybazaarConfig = emptyOfficialConfig(
  "policybazaar",
  "PolicyBazaar official careers/jobs route currently returns 404 and exposes no public job listing feed.",
);
