// Stripe — Greenhouse (boards-api.greenhouse.io/v1/boards/stripe).
// Live-verified 58 India jobs out of 492 globally.
import { greenhouseConfig } from "./_greenhouse.js";
export const stripeConfig = greenhouseConfig({ slug: "stripe", board: "stripe" });
