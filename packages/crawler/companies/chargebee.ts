import { emptyOfficialConfig } from "./_simple.js";

export const chargebeeConfig = emptyOfficialConfig(
  "chargebee",
  "Chargebee official careers page currently links openings to LinkedIn only; skipped per official-source/no-aggregator rule.",
);
